# 진단 중간 산출물 — 프롬프트 원문이 통째로 들어 있다. 절대 커밋하지 말 것
cc_usage_stats.json
*.stats.json

# 빌드 산출물 — 대시보드는 커밋하지 않고 GitHub Actions 가 매번 새로 만든다
_site/

# 로컬 툴링/IDE 런타임 상태 (추적 제외)
.omc/
.idea/
.claude/settings.local.json

# OS
.DS_Store

# Python
__pycache__/
*.py[cod]
.venv/
venv/
